import styled from "styled-components";

export const Botao2Style = styled.div`
    button {
        display: inline-block;
        background: none;
        padding: 0;
        border: none;
        cursor: pointer;
        text-align: center;
        color: #415AC5;
        font-size: 1.25rem;
        font-style: normal;
        font-weight: 600;
        line-height: normal;
    }
`